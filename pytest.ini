[pytest]
minversion = 6.0
addopts = -ra -q

# Tell pytest where to look for tests
testpaths = 
    traffic_ingestor/tests
    traffic_refresher/tests

# Add both function app folders to the Python path so imports work
pythonpath = 
    traffic_ingestor
    traffic_refresher
